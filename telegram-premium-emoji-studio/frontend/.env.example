# Frontend build-time env (Vite). Copy to .env for local dev.

# Base URL of the backend API. Leave as /api/v1 to use the Vite dev proxy.
VITE_API_BASE=/api/v1

# Where the dev proxy forwards /api requests (backend).
VITE_API_PROXY=http://localhost:8000
